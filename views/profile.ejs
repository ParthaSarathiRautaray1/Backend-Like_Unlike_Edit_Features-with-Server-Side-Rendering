<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block text-white" href="/logout">Logout</a>

        </div>
        <h3 class="text-3xl mb-5 text-green-500">Hello, <%=user.name%> Welcome To Profile ✋🏻</h3>
        <h5 class="mb-5 text-orange-500">You can Create a new Post.</h5>
        <form method="post" action="/post">
            <textarea placeholder="Whats on your mind?" class=" text-white p-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class=" px-3 w-30 mt-2 text-white rounded-md block py-2 bg-blue-500" type="submit" name="" id="" value="Create new post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5 ">

                <% user.posts.reverse().forEach(function(post){ %>

                    <div class="post w-1/3 p-5 border-[1px] mb-2  border-zinc-700 rounded-md bg-zinc-800">
                        <h4 class="text-yellow-500 mb-4"><%=user.username%></h4>
                        <p class="text-white"><%=post.content%></p>
                        <small class="mt-2 text-red-500 inline-block"><%= post.likes.length %> likes</small>
    
                        <div class=" btns flex gap-5 mt-2 ">
                            <a  href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "👍" : "👎"%>
                            </a>

                            <a  href="/edit/<%= post._id%>">📝</a>
                        </div>
    
                    </div>

                <% })%>

            </div>

        </div>
        
    </div>
    
</body>
</html>