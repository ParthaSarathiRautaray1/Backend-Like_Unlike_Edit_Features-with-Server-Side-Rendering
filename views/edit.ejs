<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block text-white" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5 text-orange-500">Edit Your Post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="Whats on your mind?" class=" text-white p-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""><%= post.content %></textarea>
            <input class=" px-3 w-30 mt-2 text-white rounded-md block py-2 bg-yellow-500" type="submit" name="" id="" value="Update post">
        </form>

       
        
    </div>
    
</body>
</html>